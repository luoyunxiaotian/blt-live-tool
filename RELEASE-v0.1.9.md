# 直播小帮手 v0.1.9（MAUI + Blazor Hybrid）

> 更新通道：MAUI 线（`v0.1.9-maui`）。
> **本版重点 = 账号代授权页面逻辑重构、重新登录与浏览器 Cookie 自动清理机制、左上角登录窗口控制视觉强化、全量异常字符清理与图标矢量化**。

---

## 一、新增功能与体验优化

### 1. 账号代授权页面场景化重构
- **未登录与未授权状态精准分离**：
  - **未登录状态**（检测无任何账号登录态）：精简呈现 4 项核心引导操作：
    1. 复制作者 B 站 ID
    2. 打开 B 站作者主页
    3. **立即登录**（HeroUI 蔚蓝晶莹高亮）
    4. **登录后点击此处进行验证**（HeroUI 翡翠绿高亮）
  - **已登录但未授权状态**（识别到当前账号不在白名单中）：提供 5 项操作：
    1. 复制作者 B 站 ID
    2. 打开 B 站作者主页
    3. **重试验证**（带频次安全限制与智能滑动验证条）
    4. **重新登录**（HeroUI 幻夜紫高亮）
    5. **清除登录状态**（警示红微胶囊按钮）
- **异常控制字符彻底清除**：
  - 深度清洗源码中遗留的不可见控制字符（SOH `\x01`），彻底修复各按钮前方可能出现的显示异常符号。
  - 新增 `shield-check` 与 `close` 等原生矢量 SVG 图标，自适应当前主题配色。

### 2. 重新登录与 Cookie 自动深度清理
- **彻底解决旧账号 Cookie 幽灵自愈问题**：
  - 原先点击“重新登录”时，因底层 WebView2 浏览器仍残留上次登录的 Cookie 与本地存储，导致登录页自动登录回旧账号。
  - 本版重构原子化【重新登录】与【清除登录状态】链路：点击时自动调用 WebView2 底层 `CookieManager` 彻底抹除所有 B 站域名 Cookie，并清理 `LocalStorage`、`IndexedDb`、`DiskCache` 与 `WebSql`。
  - 每次点击“重新登录”，必定拉起纯净空白会话并直达全新扫码/输入账号密码页面，换号体验丝滑顺畅。

### 3. 左上角登录窗口控制视觉强化与流程引导
- **文案与色彩视觉强化**：
  - 左上角按钮统一重命名为“**隐藏登录窗口**”与“**关闭登录窗口**”。
  - **关闭登录窗口**：配备高辨识度警示红徽章样式（`.blt-topstrip-btn.danger`）与矢量关闭叉号图标 `<BltIcon Name="close" />`，方便新用户在登录成功后迅速定位并关闭窗口，即刻释放 150MB~800MB 内存。
  - **隐藏登录窗口**：配备琥珀金微光高亮（`.blt-topstrip-btn.amber`）与闭眼矢量图标 `<BltIcon Name="eye-off" />`。
  - **操作引导联动**：点击登录按钮后，状态栏会主动提示：“*已打开应用内 B站 登录窗口：登录成功后可在左上角点击【关闭登录窗口】，然后点击【验证】*”，形成清晰完整的用户交互闭环。

---

## 二、兼容与升级说明
- **增量更新**：支持从 v0.1.8 无缝一键增量热更至 v0.1.9。
- **配置与数据安全**：历史配置与自定义设置完整保留。
- **脱敏安全**：全量代码与发布包经自动化脱敏扫描，0 敏感信息与私密凭据。
