# 直播小帮手 v0.1.7（MAUI + Blazor Hybrid）

> 更新通道：MAUI 线（`v0.1.7-maui`）。**本版重点 = 键鼠可视化在「某些游戏里完全不显示」的修复**：
> 采集从"一条低级钩子"改为**三条并行**（钩子 + Raw Input + 轮询），任一条被挡都有别的顶上。
> 其余内容与 0.1.6 相同。安装包为主分发物；便携 zip 行为与旧版一致。

> **增量包：内容取 0.1.3 → 0.1.7 的全量差分，起始版本标为 0.1.4**（`patch-0.1.7-from-0.1.4.zip`）。
>
> - **内容**按 0.1.3 起算（最保险）：v0.1.4 上传后被替换重发过，拿到"老 0.1.4"的机器少了一部分
>   0.1.4 的改动；从最老的已发布版本起算，差分包里一定包含它需要的全部文件。
> - **起点标 0.1.4**：客户端只有在「增量起点 == 自己的版本号」时才会使用增量包，而线上机器全是
>   0.1.4；标成 0.1.3 会让它们全部退回去下 156 MB 整包。标 0.1.4 后，老 0.1.4 与新 0.1.4 都能用
>   这条 1.6 MB 增量正确升级。
> - v0.1.5 起客户端判据放宽为「版本不低于增量起点即可」，并保留**逐文件覆盖校验**兜底
>   （差分包没覆盖本机全部差异时自动改下整包），所以起点标注与内容不完全一致也不会产出半个旧版。

## 一、修复（本版重点）

1. **OBS 抓不到本程序的声音**（迁移回归）：迁移前 Electron 版的音频由主进程**直接子进程**渲染，
   OBS 的「应用程序音频采集」（进程回环 = 目标进程 + 子进程）覆盖得到；MAUI 旧版把声音交给 WebView2，
   实际由 **`msedgewebview2.exe` 音频服务（孙进程）**发出，超出覆盖范围 → 一律静音。
   现改为用 WinRT `MediaPlayer` 在**本进程**渲染全部声音（点歌 / 面板提示音 / 语音念弹幕 / 系统语音），
   实测 OBS 应用音频采集电平 **0.3594**。原网页 `<audio>` 路径保留为兜底。
   - 排障提示：若该源的音量条一直是 0，**删掉这条源重新添加一次**（必要时重启 OBS）即可恢复；
     已武装的回环遇到「默认播放设备被切换」（Sonar/VoiceMeeter 类软件）容易僵死。
2. **语音念弹幕的音色清单不跟着引擎走**：原先无论引擎切到 moss / edge / sys，下拉里永远只有 edge 在线音色，
   且 `tts.mossVoice` 只读展示、从不写回。现按原版行为实现：moss → 本地音色（内置 + 克隆，写 `tts.mossVoice`）；
   edge/sys → 在线音色按语种分组 + 「系统音色」组（写 `tts.voice`），sys 播报时按所选音色匹配系统语音。
3. **公告通知点「我知道了 / 稍后」关不掉**：卡片过滤写成 `RepeatUntilExpire || !read`，而 `read` 只由
   「我知道了」写入 → 带「到期前重复显示」的公告永远关不掉（按钮等于摆设）；强通知的「稍后」同样只写状态、
   模态不消失。现按控制台文案重做寿命：

   | 操作 | 关掉后 | 重启应用后 |
   |---|---|---|
   | 普通通知「我知道了」 | 立即消失 | 不再出现 |
   | 「到期前重复显示」的公告「我知道了」 | 立即消失 | 继续提醒（「维护中」的用法） |
   | 强通知「稍后」(soft) | 模态立即消失 | 再弹 |
   | 强通知「我已阅读并确认」 | 不再弹 | 不再弹 |
   | 常驻横幅「收起」 | 立即消失 | 仍收起 |

   已读/收起状态同时改为按「id + 内容指纹」记：作者在控制台**编辑**已发公告后，先前关掉过它的用户会重新看到。
4. **键鼠可视化「偶尔不再响应」（本版重点）**：用户反馈"有时必须让小帮手保持前台才动"。
   实测钩子本身与前台焦点无关（前台换成资源管理器、乃至以管理员运行的 OBS，事件照样派发；已加
   `events`/`lastEventAgoMs` 计数逐条验证）。真正机制是 Windows 会**静默摘掉**低级键盘/鼠标钩子
   （回调超时、会话切换、驱动变动），且永不自动恢复 —— 现场就是"突然不响应、重启应用才回来"。
   现在加了**存活看护**：每 5 秒拿系统自己的"最后一次输入"时间（`GetLastInputInfo`，与本进程无关）
   对比我们最后一次收到事件的时间；系统说有输入而我们 ≥3 秒没收到，就在钩子线程上**自动重挂**
   （写服务日志并计入 `reinstalls`）。同时修掉一个原有 bug：`PostThreadMessage` 的 DLL 声明误写成
   `kernel32`（实际在 `user32`），导致界面上的「关闭键鼠可视化」**从来没有真正卸载钩子**。
   另外浮层重连不再只依赖定时器（页面被后台节流时定时器会冻结，断线后再也连不回来），改为断线立即
   重连 ＋ 页面恢复可见/回到前台/网络恢复时自动补连。验收 `tools/test-keyview-hook.py` 13 项全过。
6. **键鼠可视化在部分游戏里完全不显示（本版重点）**：用户反馈"大部分情况正常，玩部分游戏时键盘鼠标
   完全不显示"。实测定位到机制：那些环境里**低级钩子被系统层过滤**——诊断显示两个钩子都安装成功
   （`kb=ok ms=ok`），但**回调次数为 0**（`hookCalls=0`），即系统根本不回调我们；同一时刻另两条路照常收到事件。
   反外挂、安全软件、更高完整性的前台窗口都会造成这种过滤。
   因此采集改为**三条并行**，任一条通就行：
   - **低级钩子**（原有，最精确、坐标是绝对值）
   - **Raw Input**（`RIDEV_INPUTSINK`：系统把 `WM_INPUT` 投给**我们自己的**消息窗口，不往别人进程注入；
     放在**独立线程**，且不解析鼠标移动——移动是 500~1000Hz 的洪流，曾把钩子线程压住导致钩子被系统
     按超时摘掉）
   - **轮询兜底**（每 20ms 读一次内核的 `GetAsyncKeyState`／`GetCursorPos`，完全不经过输入链与消息投递）
   三条路的事件按指纹在 35ms 内去重，浮层不会把一次按键画成两次；`keyview/status` 与键鼠页会分别显示
   三路计数（**哪一路在涨就说明那条路通着**），钩子这一路还有独立看护（别的路在收而钩子在 3 秒内没动静
   就重挂）。
5. **键鼠可视化的总开关会被记住**：用户点过「启动」且没有手动关过，下次开程序只要授权通过就**自动恢复**，
   不必每次手点；点过「停止」则保持关闭（记录写在 `data/keyview-config.json` 的 `enabled`）。
   与之配套的**授权门禁**：未检测到授权（未登录 / 未通过白名单 / 校验未完成）时，所有功能一律保持关闭，
   此时手动点「启动」也会被拒绝（HTTP 403）；授权通过后才按本地记录恢复。
   验收 `tools/test-keyview-license.py` 9 项全过（首启不自动开 → 点启动记录 → 重启自动恢复 →
   锁定自动关且拒绝启动 → 恢复授权自动开 → 停止后重启保持关闭）。

## 二、目录结构（v0.1.4 起）

```
安装根（用户看到的全部内容）
├─ 直播小帮手.exe      启动器：双击即运行（无依赖，1.5 MB，NativeAOT）
├─ app\                程序本体与全部依赖（约 790 个文件：主程序、依赖、wwwroot、tts 引擎…）
├─ data\               用户数据（配置 / 歌词 / 录屏 / 点歌列表 / 公告与清理状态）
└─ 说明.txt            启动方式、数据位置、更新与卸载、常见问题
```

- 快捷方式指向启动器；启动器把参数透传给 `app\BiLi_live_Tool.exe` 后立刻退出（不驻留），
  托盘、单实例、开机自启仍由真正的 exe 负责。
- 数据目录通过 `app\layout.json` 标记解析到**安装根**的 `data\`；便携版（解压 zip，无标记）与旧版一致。

## 三、废弃文件清理（v0.1.4 起）

更新器是 `robocopy /E`，只能新增/覆盖、不会删除。为此补齐全链路：发布侧在清单里带**累积废弃清单**
（`obsolete`）并随包发一份手动清理脚本 `cleanup\cleanup-obsolete.cmd`；应用侧在房间管理页提供
**「清理旧文件（N）」**按钮（弹确认）。护栏：只删清单里逐条列出且位于程序目录内的路径、拒绝越界与保护名单
（`data\`、`config.json`、`layout.json`、`update_staging`、`update_backup`、WebView2 配置、启动器、说明文件）、
删除前先备份到 `update_backup\cleanup-<时间戳>\`、被占用跳过并如实报告、全程写日志与状态。

## 四、兼容与升级说明

- **从 v0.1.3（扁平布局）升级**：0.1.3 未对外分发，未做迁移路径。应用内更新只会把新文件铺进旧的扁平目录
  （拿不到「启动器 + `app\`」新布局），**要新布局请用安装包**。
- **应用内更新**：只替换 `app\` 内的文件，根目录的启动器/说明/布局标记不受影响，`data\` 与登录态保持。
- **便携 zip**：解压即用，数据仍在 exe 旁。

## 五、产物与验收

| 产物 | 大小 | SHA256 |
|---|---|---|
| `blt-live-tool-v0.1.7-maui-setup.exe` | 126.0 MB | `e621fbf06f7175425c3048264ca6ae99171b969af4c74814df9ad8cdf5560359` |
| `blt-live-tool-v0.1.7-maui-win-x64.zip` | 156.6 MB | `303fa11044d51d8bb40dd7acdba255cd1a22c46a2baa79c0343d8aed01ff5822` |
| `patch-0.1.7-from-0.1.4.zip` | 1.6 MB | `7df60f288e705092f19151820e07056bbc7457c2f1ce527472781484e527a68f` |
| `manifest-0.1.7.json` | 0.1 MB | `8b2bfdbc2ab9d0a4a5a988e9f89deadd6c89948e584ec72c612f53256b8661c8` |

验收（每次构建都会重跑）：

- `tools/verify-installer.py`（中文路径）：**36/36 通过** —— 根目录恰好 4 项、`app\` 内容完整（含 tts 与
  verify-key）、启动器能拉起应用且 HTTP 应答、数据写在安装根 `data\`、覆盖安装保留数据、
  清理专项（废弃文件被删 + 备份 + 日志 + 保护名单/越界被拒且写入日志）、卸载后 `app\`/启动器/说明/快捷方式/
  注册表项清除而 `data\` 保留。
- `tools/verify-release.py`：整包/清单/增量三件套逐字节对账 PASS。
- 脱敏（`tools/sanitize-scan.py`，以真实运行配置里的 Cookie/uid 做字面量反查）：
  **源码仓 / 整包 / 安装后 `app\` 均 0 命中**（仅作者联系 uid/昵称属有意随包）。
- 公告关闭：`tools/test-announcement-dismiss.py`（本地假公告源，四类公告 + 重启复检）**13 项全过**。
