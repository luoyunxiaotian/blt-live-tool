# 直播小帮手 v0.1.3（MAUI + Blazor Hybrid）

> 更新通道：MAUI 线（`v0.1.3-maui`）。v0.1.2 用户可直接在应用内「检查更新」升级，
> 会自动走 **增量包（1.9 MB）**，无需重下 156 MB 整包。

## 一、新增：咪咕音乐平台

点歌平台从 4 个变成 5 个（QQ / 网易云 / 酷狗 / **咪咕** / B站），搜索、播放、歌词三条链路都通了：

- **免费曲库免登录直接播放**（不需要 Cookie，也不需要登录）；
- 会员歌曲只有 60 秒试听，因此会被标为 **VIP** 并在自动点歌时跳过；
- 歌词取自咪咕曲库自带的 LRC，并作为**第三层兜底歌词源**（酷狗 → 网易云 → 咪咕），
  之前取不到词的歌（例如「起风了」）现在能拿到完整 72 行；
- 会员歌与免费歌的判定不是猜的：按 110 首样本实测得出「只有 `chargeAuditions==1` 会拒绝给地址」，
  另一档 `vipType=1` 但 `chargeAuditions=0` 的歌其实能正常播放。

## 二、修复：OBS 歌词浮层

三个独立缺陷，都会让浮层看起来"坏掉"：

1. **不滚动**：进度帧发的是 `current`，而浮层（沿用的是旧版 `song-player.js` 契约）读的是 `position`，
   于是播放位置恒为 0。现在两个字段一起发（`position` + `paused` 为主，`current`/`playing` 保留兼容）。
2. **在两首歌之间来回跳**：兜底轮询读的是"点歌请求队列游标"而不是正在播的索引，每 5 秒把卡片切回上一首。
   现在进度帧在流动时轮询完全不碰歌曲，只有连续 12 秒收不到帧才用兜底，且兜底改读播放器索引。
3. **当前句放大被两侧裁掉**：舞台宽度贴合最宽一行，放大 1.3 倍的文字被 `overflow:hidden` 切掉。
   现在行的左右内边距给了 13%（文字列宽 = 卡片 74%），实测放大后 157px < 视窗 169px，两侧各留 6px。

## 三、修复：点歌播放列表与歌词

- **「当前」徽标与「播放中」不一致**：旧版语义里 `currentIndex` 就是正在播的索引，移植时被改成了没人消费的队列游标，
  于是「当前」标在一首没在播的歌上。现在起播时同步（实测播放第 2 行 → 两枚徽标同排）。
- **「跳过当前」是哑按钮**：之前只把游标 +1，既不切歌也不影响播放；现在真的切下一首，越过最后一首则停止。
- **放完的歌不移除**（最后一首尤其明显）：现在播放结束先移除该曲再播接替位。
- **空闲时手动加歌不自动播**：现在会自动开播（与弹幕点歌一致）。
- **应用内歌词每次滚到底**：容器未定位导致 `offsetTop` 量到更远祖先的偏移，被钳到底部；改为 rect 差值计算居中。
- **网易云 VIP 误标**：`fee=8`（低音质免费，可正常播）被旧代码当成 VIP。实测 196 首搜索命中：
  `fee=1/4` 无登录 0/144 可播（全部 302 到 `music.163.com/404`），`fee=0/8` 有 54/55 直接拿到可播 CDN →
  现在只有 `fee` 为 1 或 4 才算 VIP。

## 四、新增：托盘右键菜单

托盘菜单新增 4 项（都是实时状态，未在播时置灰、开关显示勾选态）：

- **暂停点歌 / 继续点歌**、**跳过当前**；
- **自动弹幕（总开关）**、**语音念弹幕（总开关）**。

其中自动弹幕原本没有总开关（只有欢迎/礼物感谢/关注感谢/定时弹幕四个子开关），本次补上
`autoDanmu.enabled` 总门禁（**缺省为开，老配置行为不变**），页面主控条也加上了对应开关。

## 五、界面细节

- **卡片折叠三角**：原来只是一个 12px 的 `▾` 字符（墨迹仅约 8×5px），看不出能点、也看不出折叠状态。
  现在换成固定 **18×11px 的实心三角 SVG**，装在 28×28 按钮里；折叠时三角旋转成朝右并整体变成强调色，
  悬停还有「点击折叠/展开这张卡片」提示。
- 点歌平台条、Cookie 脱敏（星号 + 小眼睛）、顶部总控条等上一版的改动保持不变。

## 六、发布侧修正

- **清单与整包严格一致**：v0.1.1/v0.1.2 的 `manifest` 都漏列了 `wwwroot/blt-ui.js` 与 `wwwroot/blt-drag.js`
  （整包 788 个条目、清单只有 786 个）。本次清单直接扫描打包目录生成，788 = 788，校验脚本
  `verify-release.py` 报告 `missing=none extra=none`。
- **脱敏检查**：发布流程新增脱敏扫描（以"值形态"匹配凭据，避免误报应用自身用于识别登录态的
  标记字符串），并用**运行目录里的真实 Cookie/uid 做字面量反查**。对源码仓（173 个跟踪文件）与
  整包（788 个文件）双向扫描均为 **0 命中**；`data/`、`config.json`、WebView2 缓存与日志均不入包。

## 七、升级方式

- **应用内**：房间管理 → 「检查更新」→「更新并重启」（v0.1.2 用户仅下载 1.9 MB）。
- **手动**：下载整包解压覆盖，保留 `config.json` 与 `data/`（更新脚本会自动跳过用户数据）。

## 八、完整性

| 资产 | 大小 | SHA256 |
|------|------|--------|
| blt-live-tool-v0.1.3-maui-win-x64.zip | 156.5 MB | `c0f6ff1f7d926b2734d839ef75725273ce97fa3da22d8b0ec56a2b9c56a6dcd6` |
| patch-0.1.3-from-0.1.2.zip | 1.9 MB | `6bcf60de2e69f6b0c5e71649df1babce501a1e667fd2f6be00dc3e4004aab02e` |
| manifest-0.1.3.json | 132 KB | `0550b2326c33fe6ff53994fee5bee6f10c3794d742f05febcb15164c12841583` |

（发布机已对整包逐条对账清单：路径/大小/哈希 788/788 一致；增量包 14 个文件哈希全部匹配，
删除列表为空 —— 增量更新不会删除任何已有文件。）

> **资产重建说明（2026-09-15）**：v0.1.3 首次发布后，在验证应用内更新时发现三个缺陷并修复 ——
> ①更新按钮多打了一个 `v`、只显示整包大小（看不到增量）；②重复触发下载会把前一次取消，
> 被取消的那次用死 token 跑整包回退，报出误导性的「全部镜像均不可用：A task was canceled」；
> ③镜像竞速只看谁先回响应头，GitHub 直连赢了却只有约 20 KB/s（1.9 MB 增量包 90 秒只下 176 KB）。
> 现已按修复后的源码**重建并替换**同名三个资产（tag 不变）：按钮显示「增量 1.9 MB」、
> 重复请求不再互杀、每次尝试需在 8 秒窗口内达到 150 KB/s 否则自动换镜像
> （实测 直连 21 KB/s / ghproxy.net 19 KB/s 被弃 → ghfast.top ~2.5 MB/s，8 秒下完）。
