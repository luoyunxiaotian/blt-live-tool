# 直播小帮手 v0.1.4（MAUI + Blazor Hybrid）

> 更新通道：MAUI 线（`v0.1.4-maui`）。**本版重点是目录结构重构**：安装后根目录只有 4 项，
> 一眼就能看到能双击运行的程序。安装包为主分发物；便携 zip 行为保持不变。

## 一、新的目录结构（本版核心）

```
安装根（用户看到的全部内容）
├─ 直播小帮手.exe      启动器：双击即运行（无依赖，1.5 MB，NativeAOT）
├─ app\                程序本体与全部依赖（约 790 个文件：主程序、依赖、wwwroot、tts 引擎…）
├─ data\               用户数据（配置 / 歌词 / 录屏 / 点歌列表 / 公告缓存）
└─ 说明.txt            启动方式、数据位置、更新与卸载、常见问题
```

为什么是"启动器 + app\"：.NET 自包含程序的加载器要求运行时与依赖必须和主程序同目录
（原生 DLL 按文件名从 exe 目录加载、托管程序集按 `AppContext.BaseDirectory` 探测），
把约 700 个依赖搬到别处需要自定义探测与解析器，风险高、收益小。改用启动器后，788 个文件
收敛成"一个能双击的 exe + 三个清楚的项"。

- 快捷方式指向启动器；启动器把参数透传给 `app\BiLi_live_Tool.exe` 后立刻退出（不驻留），
  托盘、单实例、开机自启仍由真正的 exe 负责。
- 数据目录通过 `app\layout.json` 标记解析到**安装根**的 `data\`：便携版（解压 zip，无标记）
  行为与以前完全一致，数据仍在 exe 旁。

## 二、新增：废弃文件清理（解决"更新只能覆盖、不能删除"）

旧版更新器的实现是 `robocopy /E`，**只能新增/覆盖文件，不会删除**，所以一旦某个版本删掉或
改名了文件，那些文件会永远留在安装目录里。现在补齐了整条链路：

1. **发布侧**：打包时计算"上一版有、这一版没有"的路径，维护一份**累积废弃清单**，写进
   `manifest`（字段 `obsolete`），并把一份**可手动运行的清理脚本** `cleanup\cleanup-obsolete.cmd`
   随包发出。
2. **应用侧**：检查更新时把清单存到 `data\pending-cleanup.json`；更新完成后，房间管理页出现
   **「清理旧文件（N）」按钮**，点一下即清理（弹确认框）。
3. **安全护栏**（只做"删除清单里逐条列出的文件"，绝不通配/扫描）：
   - 路径必须规范化后仍落在程序目录内（`..\` 逃逸与绝对路径一律拒绝）；
   - 保护名单硬编码：`data\`、`config.json`、`layout.json`、`update_staging`、`update_backup`、
     WebView2 配置目录、启动器、`说明.txt`、清理脚本自身；
   - 删除前先复制到 `update_backup\cleanup-<时间戳>\`，可后悔；
   - 被进程占用的文件跳过并如实报告，留到下次；
   - 每次清理写 `data\cleanup-log.txt` 与 `data\cleanup-state.json`，可追溯。

## 三、修复

- 公告缓存/状态文件与托盘「打开数据目录」原先硬编码 `exe目录\data`，在新布局下会指错位置，
  已统一走数据目录解析。
- **OBS「应用程序音频捕获」抓不到本程序的声音（重要）**：旧实现把点歌 / 提示音 / 语音念弹幕
  全部交给网页里的 `<audio>` 元素播放，而 WebView2 会把声音交给 Chromium 自己的沙箱子进程
  （`msedgewebview2.exe --utility-sub-type=audio.mojom.AudioService`）渲染——那是**本程序的孙进程**。
  OBS 的应用程序音频捕获是 WASAPI 进程回环，抓的是"被选进程自己的音频会话"，所以在 OBS 里选
  「直播小帮手」时那个进程一条音频会话都没有，只会录到静音（回环设备本身是初始化成功的）。
  现在改为由 `Services/NativeAudio.cs` 用 WinRT `MediaPlayer` 在**本进程**内渲染全部声音，
  音频会话的 PID 就等于程序 PID，OBS 里用原来的配置（选 直播小帮手 / `BiLi_live_Tool.exe`）
  即可收到声音，无需虚拟声卡。网页 `<audio>` 那条路保留为兜底（引擎不可用时自动回退）。
  详细原理与复现命令见 `开发文档/OBS音频捕获修复.md`。
- **语音念弹幕：音色清单不跟着引擎走**：原先无论引擎切到 moss / edge / sys，下方「音色与试听」
  下拉里永远只有 edge 的在线音色（选 moss 看不到本地音色与克隆音色，选 sys 没有「系统音色」可选），
  且 `tts.mossVoice` 只读展示、从不写回配置。现按原版（Electron 版 `rebuildVoiceSelect`）实现：
  moss → 本地音色（内置 + 克隆，写 `tts.mossVoice`）；edge/sys → 在线音色按语种分组 + 「系统音色」组
  （写 `tts.voice`），sys 播报时按所选音色匹配系统语音、匹配不到退中文音色。系统音色由本进程的
  语音合成器提供（新增 `GET /api/tts/sys/voices`），试听也按引擎分流。详见 `开发文档/TTS音色与引擎选择.md`。

## 四、兼容与升级说明

- **从 v0.1.3（扁平布局）升级**：v0.1.3 未对外分发，无需迁移路径。若你本地有旧的扁平安装，
  建议先卸载再装本版（安装器不会自动删除旧的散落文件）。
- **应用内更新**：只替换 `app\` 内的文件，根目录的启动器/说明/布局标记不受影响，`data\` 与
  登录态保持；本版已具备"删除废弃文件"能力，后续版本清理可以一键完成。
- **便携 zip**：结构与 0.1.3 相同（无 `layout.json` 标记），数据仍在 exe 旁。

## 五、产物与验收

| 产物 | 大小 | SHA256 |
|---|---|---|
| `blt-live-tool-v0.1.4-maui-setup.exe` | 126.0 MB | `488948c44d7268788025d01a9bcddd37acb42f93f0003316cf6ee9e37dacfec5` |
| `blt-live-tool-v0.1.4-maui-win-x64.zip` | 156.6 MB | `441fbff21da6dc1975caa7504396075bcb78936cd48ee5b818888ad9d21db60e` |
| `patch-0.1.4-from-0.1.3.zip` | 1.6 MB | `8b79114970c5bf6473d6effa2d433f92fdfe76c30433ab23caecbcdf8bc52fae` |
| `manifest-0.1.4.json` | 131 KB | `3ab7367e454074542b10a252eeb250ba1b9d64e5d3420b081c0f495838073f1f` |

> 上表是**含音频捕获修复与 TTS 音色修复**的那次构建（本版资产在修复后重建过两次，下载请以
> 本表哈希为准；旧哈希已作废）。

验收（`tools/verify-installer.py`，中文路径）：**36/36 通过** —— 根目录恰好 4 项、`app\`
内容完整（含 tts 与 verify-key）、启动器能拉起应用且 HTTP 应答、数据写在安装根 `data\`、
覆盖安装保留数据、清理专项（废弃文件被删 + 备份 + 日志 + 保护名单/越界被拒且写入日志）、
卸载后 `app\`/启动器/说明/快捷方式/注册表项清除而 `data\` 保留。

脱敏（`tools/sanitize-scan.py`，以你的真实运行配置里的 Cookie/uid 做字面量反查）：
**源码仓 181 个跟踪文件 0 命中 · 整包 zip 790 文件 0 命中 · 安装后 `app\` 793 文件 0 命中**
（仅 2~6 处「作者联系 uid/昵称」属有意随包）。另外 `tools/make-installer.py` 新增护栏：
编译前会清掉负载目录里的运行时残留（`data\`、`<exe>.WebView2\`、`config.json`）——
应用一旦在负载目录里被启动过就会写这些（里面是登录态与浏览器配置），绝不能进安装包。
另单独回归了**便携版**：解压 zip 后 exe 在根、无布局标记，写入配置后数据落在 exe 旁
（不会跑到上一级）。

## 六、已知限制

- 安装包**未做代码签名**，首次运行会有 SmartScreen「未知发布者」提示。
- 缺少 WebView2 运行时的系统：安装时检测并提示（不捆绑，Win11/装过 Edge 的 Win10 自带）。
- 应用内更新无法搬动既有安装的目录结构（更新器只能写 exe 所在目录）；本次布局变更由安装包完成，
  之后的结构调整请继续用安装包，或提前把迁移机制并入更新器。
- `app\` 内部仍是"exe 相对"的扁平结构（依赖必须与主程序同目录），不影响根目录观感。
