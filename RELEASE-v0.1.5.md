# 直播小帮手 v0.1.5（MAUI + Blazor Hybrid）

> 更新通道：MAUI 线（`v0.1.5-maui`）。**本版是 v0.1.4 的补发版**（0.1.4 只上传过、未对外分发），
> 内容 = 0.1.4 的目录结构重构 + 本轮修掉的三处问题。安装包为主分发物；便携 zip 行为与旧版一致。

> **增量包基于 0.1.3**（`patch-0.1.5-from-0.1.3.zip`）：v0.1.4 上传后被替换重发过，当时拿到"老 0.1.4"
> 的人版本号与新版相同、永远等不到 0.1.4 的更新；把增量做成「0.1.3 → 0.1.5」的全量差分后，
> 这类机器也能升上来 —— 客户端判据放宽为「版本不低于增量起点即可」，并保留逐文件覆盖校验兜底
> （校验不过自动改下整包）。

## 一、修复（本版重点）

1. **OBS 抓不到本程序的声音**（迁移回归）：迁移前 Electron 版的音频由主进程**直接子进程**渲染，
   OBS 的「应用程序音频采集」（进程回环 = 目标进程 + 子进程）覆盖得到；MAUI 旧版把声音交给 WebView2，
   实际由 **`msedgewebview2.exe` 音频服务（孙进程）**发出，超出覆盖范围 → 一律静音。
   现改为用 WinRT `MediaPlayer` 在**本进程**渲染全部声音（点歌 / 面板提示音 / 语音念弹幕 / 系统语音），
   实测 OBS 应用音频采集电平 **0.3594**。原网页 `<audio>` 路径保留为兜底。
   - 排障提示：若该源的音量条一直是 0，**删掉这条源重新添加一次**（必要时重启 OBS）即可恢复；
     已武装的回环遇到「默认播放设备被切换」（Sonar/VoiceMeeter 类软件）容易僵死。
2. **语音念弹幕的音色清单不跟着引擎走**：原先无论引擎切到 moss / edge / sys，下拉里永远只有 edge 在线音色，
   且 `tts.mossVoice` 只读展示、从不写回。现按原版行为实现：moss → 本地音色（内置 + 克隆，写 `tts.mossVoice`）；
   edge/sys → 在线音色按语种分组 + 「系统音色」组（写 `tts.voice`），sys 播报时按所选音色匹配系统语音。
3. **公告通知点「我知道了 / 稍后」关不掉**：卡片过滤写成 `RepeatUntilExpire || !read`，而 `read` 只由
   「我知道了」写入 → 带「到期前重复显示」的公告永远关不掉（按钮等于摆设）；强通知的「稍后」同样只写状态、
   模态不消失。现按控制台文案重做寿命：

   | 操作 | 关掉后 | 重启应用后 |
   |---|---|---|
   | 普通通知「我知道了」 | 立即消失 | 不再出现 |
   | 「到期前重复显示」的公告「我知道了」 | 立即消失 | 继续提醒（「维护中」的用法） |
   | 强通知「稍后」(soft) | 模态立即消失 | 再弹 |
   | 强通知「我已阅读并确认」 | 不再弹 | 不再弹 |
   | 常驻横幅「收起」 | 立即消失 | 仍收起 |

   已读/收起状态同时改为按「id + 内容指纹」记：作者在控制台**编辑**已发公告后，先前关掉过它的用户会重新看到。

## 二、目录结构（v0.1.4 起）

```
安装根（用户看到的全部内容）
├─ 直播小帮手.exe      启动器：双击即运行（无依赖，1.5 MB，NativeAOT）
├─ app\                程序本体与全部依赖（约 790 个文件：主程序、依赖、wwwroot、tts 引擎…）
├─ data\               用户数据（配置 / 歌词 / 录屏 / 点歌列表 / 公告与清理状态）
└─ 说明.txt            启动方式、数据位置、更新与卸载、常见问题
```

- 快捷方式指向启动器；启动器把参数透传给 `app\BiLi_live_Tool.exe` 后立刻退出（不驻留），
  托盘、单实例、开机自启仍由真正的 exe 负责。
- 数据目录通过 `app\layout.json` 标记解析到**安装根**的 `data\`；便携版（解压 zip，无标记）与旧版一致。

## 三、废弃文件清理（v0.1.4 起）

更新器是 `robocopy /E`，只能新增/覆盖、不会删除。为此补齐全链路：发布侧在清单里带**累积废弃清单**
（`obsolete`）并随包发一份手动清理脚本 `cleanup\cleanup-obsolete.cmd`；应用侧在房间管理页提供
**「清理旧文件（N）」**按钮（弹确认）。护栏：只删清单里逐条列出且位于程序目录内的路径、拒绝越界与保护名单
（`data\`、`config.json`、`layout.json`、`update_staging`、`update_backup`、WebView2 配置、启动器、说明文件）、
删除前先备份到 `update_backup\cleanup-<时间戳>\`、被占用跳过并如实报告、全程写日志与状态。

## 四、兼容与升级说明

- **从 v0.1.3（扁平布局）升级**：0.1.3 未对外分发，未做迁移路径。应用内更新只会把新文件铺进旧的扁平目录
  （拿不到「启动器 + `app\`」新布局），**要新布局请用安装包**。
- **应用内更新**：只替换 `app\` 内的文件，根目录的启动器/说明/布局标记不受影响，`data\` 与登录态保持。
- **便携 zip**：解压即用，数据仍在 exe 旁。

## 五、产物与验收

| 产物 | 大小 | SHA256 |
|---|---|---|
| `blt-live-tool-v0.1.5-maui-setup.exe` | 126.0 MB | `9231db3240a52388c210c834afabfed8d99f5d52cdc1a7bba7dd0f3a58f0f4fb` |
| `blt-live-tool-v0.1.5-maui-win-x64.zip` | 156.6 MB | `41c6ef0041dc15c3c7c3f352bfce72325f4eb705e4a8be6dde92a2b445cf0c88` |
| `patch-0.1.5-from-0.1.3.zip` | 1.6 MB | `b19ef5d6ab3346e2fd63f8a9578a35463a67b5b3c28a6d0d4de27786f19496fd` |
| `manifest-0.1.5.json` | 0.1 MB | `0a53cf193405edd518e8482d33aa25939836fcdfa0f1a5af1368f9caf5669ed4` |

验收（每次构建都会重跑）：

- `tools/verify-installer.py`（中文路径）：**36/36 通过** —— 根目录恰好 4 项、`app\` 内容完整（含 tts 与
  verify-key）、启动器能拉起应用且 HTTP 应答、数据写在安装根 `data\`、覆盖安装保留数据、
  清理专项（废弃文件被删 + 备份 + 日志 + 保护名单/越界被拒且写入日志）、卸载后 `app\`/启动器/说明/快捷方式/
  注册表项清除而 `data\` 保留。
- `tools/verify-release.py`：整包/清单/增量三件套逐字节对账 PASS。
- 脱敏（`tools/sanitize-scan.py`，以真实运行配置里的 Cookie/uid 做字面量反查）：
  **源码仓 / 整包 / 安装后 `app\` 均 0 命中**（仅作者联系 uid/昵称属有意随包）。
- 公告关闭：`tools/test-announcement-dismiss.py`（本地假公告源，四类公告 + 重启复检）**13 项全过**。
