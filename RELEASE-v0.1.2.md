# 直播小帮手 v0.1.2（MAUI + Blazor Hybrid）

> 更新通道：MAUI 线（`v0.1.2-maui`）。v0.1.1 用户可直接在应用内「检查更新」升级，
> 会自动走 **增量包（1.3 MB）**，无需重下 156 MB 整包。

## 一、应用内更新加速：多镜像 + 竞速下载（本次重点）

把 Electron 旧版 `updater.js` 的多镜像实现完整移植到 C#：

- **版本检查**：走 7 个 API 镜像，官方 `api.github.com` 优先，失败按列表顺序回退（每个 15 秒超时）。
- **更新包下载**：8 个下载镜像 **并行发起、最快响应者胜出**，其余立即取消；下载状态栏会显示实际来源（例如 `（ghfast.top）`）与"本地已下载并校验通过，跳过重复下载"。
- **已下载缓存复用**：上一次中途失败/被取消的包，只要大小与 SHA256 仍匹配就直接复用，不再重下。

本机实测（同一时刻对同一资产的真实测速，供参考）：

| 线路 | 小文件 (133 KB manifest) | 大文件 (Range 6 MB 测速) |
|------|--------------------------|---------------------------|
| 官方直连 github.com | 302 → 存储节点约 55 KB/s | ~55 KB/s |
| **ghfast.top** | ✅ 200 | **~2 MB/s（约 36×）** |
| gh-proxy.com | ✅ 200 | ~270 KB/s |
| ghproxy.net | ✅ 200 | ~54 KB/s |
| gh.jasonzeng.dev / gitproxy.dev / mirror.ghproxy.com | ✗ 无响应 | ✗ 无响应 |
| github.akams.cn | ✗ 404 | ✗ 404 |

失效镜像会被竞速自动跳过，不影响可用性；镜像只影响速度，**不影响完整性**：更新包仍按
发布清单里的 SHA256 逐文件校验，校验失败即丢弃并回退整包。

## 二、增量更新落地

- 发布三件套：整包 `blt-live-tool-v0.1.2-maui-win-x64.zip`（156.3 MB）+ `manifest-0.1.2.json` + `patch-0.1.2-from-0.1.1.zip`（**1.30 MB**）。
- v0.1.1 → v0.1.2 只改动 7 个文件：`BiLi_live_Tool.dll`、`BiLi_live_Tool.exe`、`BiLi_live_Tool.deps.json`、`staticwebassets.endpoints.json`、`wwwroot/blt.css`、`wwwroot/themes.css`、`wwwroot/index.html`。
- 删除列表为空 —— 与 v0.1.1 文件集完全一致（786 项），增量更新不会删除任何已有文件（含 `tts/` 引擎与 `verify-key.txt`）。
- 跨版本（例如从 v0.1.0 直接升）会自动回退整包。

## 三、界面优化（三轮）

1. **可读性**：语义色文字 token（`--{green,red,amber,blue}-ink`），9 套皮肤全部满足 WCAG AA，实测**最低对比度 4.61:1**（修前有 57 处低于 4.5:1，最低 1.32:1）；焦点环可见、交互反馈、命中区下限；`prefers-reduced-motion` 支持；1040/780 px 窄窗自动堆叠。
2. **图标系统**：全站 emoji → **51 个自制线性 SVG 图标**（随主题着色、随字号缩放）。
3. **侧栏辨识度**：分组标题改为区块标签（浅底横幅 + 左侧色条 + 分隔线 + 11.5px 加粗字距），折叠箭头由 11px 字形升级为 **15px 粗描边 SVG 雪佛龙**（展开朝下 / 折叠朝右）；**当前页所在分组高亮**；**跳转到被折叠分组时自动展开**（补齐旧版行为）。

## 四、升级方式

- **应用内**：房间管理 → 「检查更新」→「更新并重启」（v0.1.1 用户仅下载 1.3 MB）。
- **手动**：下载整包解压覆盖，保留 `config.json` 与 `data/`（更新脚本也会自动跳过用户数据）。

## 五、完整性

| 资产 | 大小 | SHA256 |
|------|------|--------|
| blt-live-tool-v0.1.2-maui-win-x64.zip | 156.3 MB | `cb3753d160dd674880e7723e2485fbb120225084db69ca8f376624f43d406fdc` |
| patch-0.1.2-from-0.1.1.zip | 1.30 MB | `fa908728498b311f72fd3d7c2422ec032a3d33123e0e5d8d8ddb88d481222927` |
| manifest-0.1.2.json | 130 KB | `3c10e0003a06867ff9e366ec67a8645b7baaf3c31417d5d9c0017b0bee3c10a3` |

（发布机已对整包逐条对账 manifest：路径/大小/哈希 786/786 一致；增量包 7 文件哈希全部匹配。）
