<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BiLi_live_Tool.MainPage">

    <!-- The migrated panel UI is the whole window (served by the embedded
         Kestrel). The bilibili live browser is a separate owned borderless
         window docked over the right ~62% — the same model as the Electron
         version's WebContentsView. -->
    <WebView x:Name="panelWebView" BackgroundColor="#14161a" />

</ContentPage>
